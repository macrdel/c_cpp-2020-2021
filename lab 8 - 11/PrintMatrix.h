#pragma once
#include "IntMatrix.h"
#include <iostream>

void OutputMatrixVer1(const IntMatrix& x, const char*, FILE*);

void OutputMatrixVer2(const IntMatrix& x, const char*, FILE*);

void RandomFill(IntMatrix& x, char, FILE*);